{
  "name": "conflict_glob_visualizer",
  "version": "0.0.1",
  "scripts": {
    "build": "webpack --config webpack.config.js", 
    "deploy": "gh-pages -d dist",
    "start": "webpack serve --config webpack.config.js --open --port 5000"
  }
}
